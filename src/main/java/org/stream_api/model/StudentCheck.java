package org.stream_api.model;

/**
 * @FuntionalInterface
 * StudentCheck - функциональный интерфейс, так как он содержит только один метод
 */

public interface StudentCheck {

    boolean check(Student student);
}
